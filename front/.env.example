# Leave empty in local development to make browser requests use relative paths
# and go through the Vite dev proxy.
VITE_API_BASE_URL=

# Vite dev server proxy target for /api and /ping.
VITE_DEV_PROXY_TARGET=http://localhost:8080
