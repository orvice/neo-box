.env
.claude/
.gocache/
.gomodcache/
bin/
tmp/
config/
.DS_Store

# Frontend toolchain is npm (Dockerfile uses `npm ci`, package-lock.json
# is the tracked lockfile). Reject pnpm/yarn artifacts.
front/pnpm-lock.yaml
front/yarn.lock
front/pnpm-workspace.yaml
front/.vite/
data/
